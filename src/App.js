import CommentsList from './components/CommentsList/index';
import * as Styled from './utils/global-styles';

const App = () => {
  return (
    <Styled.WrapperBlockApp>
      <CommentsList />
    </Styled.WrapperBlockApp>
  );
}

export default App;
